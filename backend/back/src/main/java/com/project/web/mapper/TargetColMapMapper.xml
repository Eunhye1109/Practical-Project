<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.project.web.mapper.TargetColMapMapper">

  <!-- DB에 존재하는 매핑인지 확인 (1개만 반환) -->
  <select id="findMappedCol" resultType="string">
    SELECT matched_col
    FROM target_col_map
    WHERE target_col = #{targetCol}
      AND matched_col IN
      <foreach item="col" collection="candidateCols" open="(" separator="," close=")">
        #{col}
      </foreach>
    FETCH FIRST 1 ROWS ONLY
  </select>

  <!-- 새 매핑 저장 -->
  <insert id="saveMapping">
    INSERT INTO target_col_map (target_col, matched_col)
    VALUES (#{targetCol}, #{matchedCol})
  </insert>

</mapper>
